// ! values should be based on MovieItemInterface property keys

export enum SearchToggleEnum {
  TITLE = "title",
  GENRE = "genre",
}

export enum SortToggleEnum {
  REL_DATE = "relDate",
  RATING = "rating",
}
