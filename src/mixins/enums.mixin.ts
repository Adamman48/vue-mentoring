import {
  SearchToggleEnum,
  SortToggleEnum,
} from "@/definitions/MainPage.definitions";
import { SizeEnum } from "@/definitions/MyLogo.definitions";

export const enums = {
  data() {
    return {
      SizeEnum,
      SearchToggleEnum,
      SortToggleEnum,
    };
  },
};
